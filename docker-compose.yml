
version: '3.8'
services:
  postgres:
    image: postgres:14-alpine
    ports:
      - "54321:5432"
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=vinku
